@model AccountDetailsViewModel

@{
    ViewData["Title"] = "Delete";
}
<div class="container mt-5">
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h2 class="text-center text-danger mb-4">Delete Account Details</h2>
            <hr />
            <div class="row">
                <div class="col-md-6">
                    <dl class="row">
                        <dt class="col-sm-4 fw-bold text-muted">
                            @Html.DisplayNameFor(model => model.Emp_Code)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.Emp_Code)
                        </dd>

                        <dt class="col-sm-4 fw-bold text-muted">
                            @Html.DisplayNameFor(model => model.BankName)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.BankName)
                        </dd>

                        <dt class="col-sm-4 fw-bold text-muted">
                            @Html.DisplayNameFor(model => model.BankAccountNumber)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.MaskedBankAccountNumber)
                        </dd>
                    </dl>
                </div>

                <div class="col-md-6">
                    <dl class="row">
                        <dt class="col-sm-4 fw-bold text-muted">
                            @Html.DisplayNameFor(model => model.UANNumber)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.MaskedUanNumber)
                        </dd>

                        <dt class="col-sm-4 fw-bold text-muted">
                            @Html.DisplayNameFor(model => model.PFAccountNumber)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.MaskedPfAccountNumber)
                        </dd>
                    </dl>
                </div>
            </div>

            <div class="mt-4 d-flex justify-content-between">
                <form asp-action="DeleteConfirm" class="d-inline">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
                <a asp-action="Index" class="btn btn-outline-secondary">Back to List</a>
            </div>
        </div>
    </div>
</div>