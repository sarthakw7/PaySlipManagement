@model EmployeeTypeViewModel
@{
    ViewData["Title"] = "Create";
}
<h1 class="form-heading">Create Employee Type</h1>
<hr class="custom-hr" />

<div class="container form-container">
    <form asp-action="Create">
        <div class="form-group">
            <label asp-for="EmpType" class="form-label"></label>
            <input asp-for="EmpType" class="form-control input-field" placeholder="Enter Employee Type" />
        </div>

        <div class="form-group">
            <label asp-for="LeaveAllocation" class="form-label"></label>
            <input asp-for="LeaveAllocation" class="form-control input-field" placeholder="Enter Leave Allocation" />
        </div>

        <div class="form-group mt-4">
            <button type="submit" class="btn btn-primary">Create</button>
        </div>
    </form>

    <div class="form-group mt-2">
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </div>
</div>

<style>
    .form-heading {
        font-size: 2rem;
        color: #5dcaff;
        font-weight: 600;
        text-align: center;
        margin-bottom: 10px;
    }

    .custom-hr {
        border: 0;
        height: 2px;
        background: #ffa726; /* Slight orange tint */
        margin: 20px auto;
    }

    .form-container {
        max-width: 400px; /* Set a max width for the form */
        margin: 0 auto;
        padding: 20px;
        background-color: #f9f9f9;
        border-radius: 12px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .form-label {
        font-weight: 500;
        color: #444;
    }

    .input-field {
        border-radius: 8px;
        padding: 10px;
        font-size: 1rem;
        border: 1px solid #ccc;
        width: 100%;
        transition: border-color 0.3s;
    }

    .input-field:focus {
        border-color: #5dcaff;
        outline: none;
        box-shadow: 0 0 4px rgba(93, 202, 255, 0.5);
    }

    .btn-primary {
        border-radius: 8px;
        padding: 10px 20px;
        font-size: 1rem;
        font-weight: 500;
        transition: background-color 0.3s;
    }

    .btn-secondary {
        border-radius: 8px;
        padding: 10px 20px;
        font-size: 1rem;
        font-weight: 500;
        transition: background-color 0.3s;
    }

    .btn-secondary:hover {
        background-color: #e2e2e2;
    }
</style>


