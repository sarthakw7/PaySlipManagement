@model LoginViewModel

@{
    ViewData["Title"] = "Login";
}
<div class="container mt-4">
    <div class="row d-flex justify-content-center align-items-center ">
        <div class="col col-md-8 col-lg-6 col-xl-5">
            <div class="card" style="border-radius: 1rem;">
                <div class="card-body p-4 p-lg-5 text-black">
                    <form asp-action="Login" method="post" class="login-form">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="text-center mb-3 pb-1">
                            <i class="fas fa-sign-in fa-3x me-3" style="color: #ff6219;"></i>
                            <h1 class="h1 fw-bold">@ViewData["Title"]</h1>
                        </div>

                        <div class="form-outline mb-3">
                            <span class="fa fa-user"></span>
                            <label asp-for="Emp_Code" class="form-label">Emp Code</label>
                            <b><span class="text-danger">*</span></b>
                            <input asp-for="Emp_Code" class="form-control form-control-sm" />
                        </div>

                        <div class="form-outline mb-4">
                            <span class="fa fa-lock"></span>
                            <label asp-for="Password" class="form-label">Password</label>
                            <b><span class="text-danger">*</span></b>
                            <input asp-for="Password" type="password" class="form-control form-control-sm" />
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-success btn-sm">Login</button>
                        </div>

                        <div class="text-center mt-3">
                            <div>
                                <a asp-action="ForgotPassword">Forgot Password?</a>
                            </div>                            <p class="mb-2 pb-lg-2" style="color: #393f81;">
                                Don't have an account? <a href="/Account/Register" style="color: #393f81;">Register here</a>
                            </p>
                            <a href="#!" class="small text-muted">Terms of use.</a>
                            <a href="#!" class="small text-muted">Privacy policy</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


























@* @* @model LoginViewModel

@{
    ViewData["Title"] = "Login";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-5 col-md-6">
            <div class="panel border bg-white py-2">
                <div class="panel-heading">
                    <h2 class="pt-3 font-weight-bold login-title">@ViewData["Title"]</h2>
                </div>
                <form asp-action="Login" method="post" class="login-form">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group pt-3">
                        <div class="input-field">
                            <span class="fa fa-user"></span>
                            <label asp-for="Emp_Code" class="form-label">EmpCode</label>
                            <span class="text-danger">*</span>
                            <input asp-for="Emp_Code" class="form-control" />
                        </div>
                    </div>
                    <div class="form-group pt-2">
                        <div class="input-field">
                            <span class="fa fa-lock"></span>
                            <label asp-for="Password" class="form-label">Password</label>
                            <span class="text-danger">*</span>
                            <input asp-for="Password" type="password" class="form-control" />
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="form-inline justify-content-between">
                            <a id="forget" class="font-weight-bold" href="#">Forgot Password?</a>
                            <button type="submit" class="btn btn-primary login-button">Login</button>
                        </div>
                    </div>
                    <div class="text-center py-3 text-muted">
                        Don't have an Account? <a id="register-link" href="/Account/Register">Create an Account</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Any additional scripts can be placed here
    </script>
}

<style>
    .panel-heading {
        text-align: center;
        margin-bottom: 15px;
    }

    .input-field {
        position: relative;
    }

        .input-field .fa {
            position: absolute;
            top: 50%;
            left: 20px;
            transform: translateY(-50%);
        }

    .loginbtn {
        text-align: center;
    }

    #register-link {
        margin-top: 10px;
        display: inline-block;
    }
</style>
 *@ 
@* 
@model LoginViewModel

@{
    ViewData["Title"] = "Login";
}
<div class="container h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
        <div class="col col-md-8 col-lg-6 col-xl-5">
            <div class="card" style="border-radius: 1rem;">
                <div class="card-body p-4 p-lg-5 text-black">
                    <form asp-action="Login" method="post" class="login-form">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="text-center mb-3 pb-1">
                            <i class="fas fa-sign-in fa-3x me-3" style="color: #ff6219;"></i>
                            <h1 class="h1 fw-bold">@ViewData["Title"]</h1>
                        </div>

                        <div class="form-outline mb-3">
                            <span class="fa fa-user"></span>
                            <label asp-for="Emp_Code" class="form-label">Emp Code</label>
                            <b><span class="text-danger">*</span></b>
                            <input asp-for="Emp_Code" class="form-control form-control-sm" />
                        </div>

                        <div class="form-outline mb-4">
                            <span class="fa fa-lock"></span>
                            <label asp-for="Password" class="form-label">Password</label>
                            <b><span class="text-danger">*</span></b>
                            <input asp-for="Password" type="password" class="form-control form-control-sm" />
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-success btn-sm">Login</button>
                        </div>

                        <div class="text-center mt-3">
                            <a href="#!" class="small text-muted">Forgot password?</a>
                            <p class="mb-2 pb-lg-2" style="color: #393f81;">
                                Don't have an account? <a href="/Account/Register" style="color: #393f81;">Register here</a>
                            </p>
                            <a href="#!" class="small text-muted">Terms of use.</a>
                            <a href="#!" class="small text-muted">Privacy policy</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div> *@


