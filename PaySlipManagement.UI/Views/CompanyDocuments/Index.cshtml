@*@model IEnumerable<CompanyDocumentsViewModel>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Company Documents Management</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f7fafc;
        }

        .form-container {
            max-width: md;
            margin: auto;
            padding: 2rem;
            background-color: white;
            border: 1px solid #e2e8f0;
            border-radius: 0.5rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .form-group label {
            font-weight: 600;
        }

        .btn {
            transition: background-color 0.2s ease;
        }

        .btn-download {
            background-color: #4c51bf;
            color: white;
        }

            .btn-download:hover {
                background-color: #434190;
            }

        .btn-view {
            background-color: #718096;
            color: white;
        }

            .btn-view:hover {
                background-color: #4a5568;
            }
    </style>
</head>
<body>
    <div class="form-container mt-10">
        <h2 class="text-lg font-semibold text-gray-800 mb-4 text-center">Company Documents Management</h2>

        <div class="form-group mb-6">
            <div class="card shadow-lg border-light">
                <div class="card-body bg-light">
                    <form method="post" action="@Url.Action("Index")">
                        <div class="form-group">
                            <label for="employeeCode" class="font-weight-bold text-muted">Employee:</label>
                            <div class="input-group">
                                <div class="input-group-prepend">
                                    <span class="input-group-text bg-info text-white">
                                        <i class="fas fa-id-badge"></i>
                                    </span>
                                </div>
                                <select id="employeeCode" name="Emp_Code" class="custom-select">
                                    <option value="">Select Employee</option>
                                    @if (ViewBag.Employees != null)
                                    {
                                        foreach (var employee in ViewBag.Employees)
                                        {
                                            <option value="@employee.Emp_Code">@employee.Emp_Code</option>
                                        }
                                    }
                                </select>
                            </div>
                        </div>

                        <label for="documentType" class="block text-sm font-medium text-gray-700">Document Type</label>
                        <select id="documentType" name="DocumentType" required class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
                            <option value="">-- Select Document Type --</option>
                            <option value="Company UAN">Company UAN</option>
                            <option value="GST/MOI/COI/PAN/TAN/ICC">GST/MOI/COI/PAN/TAN/ICC</option>
                            <option value="Rental Agreements">Rental Agreements</option>
                            <option value="Broadband services">Broadband services</option>
                            <option value="Coffee/Tea vendors">Coffee/Tea vendors</option>
                            <option value="House keeping">House keeping</option>
                            <option value="Stationary">Stationary</option>
                            <option value="Welcome kit">Welcome kit</option>
                            <option value="Laptop">Laptop</option>
                            <option value="Electricity">Electricity</option>
                            <option value="Water Bill">Water Bill</option>
                            <option value="AC Bill">AC Bill</option>
                            <option value="Rental Bills">Rental Bills</option>
                            <option value="Others">Others</option>
                        </select>

                        <div class="form-group mt-4 text-center">
                            <button type="submit" class="btn btn-info btn-block text-white">
                                <i class="fas fa-filter"></i> Apply Filters
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        @*<div class="flex justify-between">
            <form asp-action="DownloadDocument" method="post" class="flex-1 mr-2">
                <input type="hidden" name="documentType" id="hiddenDocumentType" value="" />
                <button type="submit" class="btn btn-download w-full py-2 rounded-md">
                    <i class="bi bi-file-earmark-arrow-down"></i> Download
                </button>
            </form>

            <form asp-action="ViewDocument" method="post" class="flex-1 ml-2">
                <input type="hidden" name="documentType" id="hiddenDocumentTypeView" value="" />
                <button type="submit" class="btn btn-view w-full py-2 rounded-md">
                    <i class="bi bi-eye"></i> View
                </button>
            </form>
        </div>
    </div>

    <table class="table table-bordered mt-4">
        <thead>
            <tr>
                <th>Id</th>
                <th>Employee Code</th>
                <th>Document Type</th>
                <th>File Type</th>
                <th>File Name</th>
                <th>Actions</th> <!-- Added Actions column -->
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var task in Model)
                {
                    <tr>
                        <td>@task.Id</td>
                        <td>@task.Emp_Code</td>
                        <td>@task.DocumentType</td>
                        <td>@task.FileType</td>
                        <td>@task.FileName</td>
                        <td>
                            <!-- View Button -->
                            <form method="post" asp-action="ViewDocument">
                                <input type="hidden" name="id" value="@task.Id" />
                                <button type="submit" class="btn btn-view px-3 py-1 rounded-md">
                                    <i class="bi bi-eye"></i> View
                                </button>
                            </form>

                            <!-- Download Button -->
                            <form method="post" asp-action="DownloadDocument">
                                <input type="hidden" name="id" value="@task.Id" />
                                <button type="submit" class="btn btn-download px-3 py-1 rounded-md">
                                    <i class="bi bi-file-earmark-arrow-down"></i> Download
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="5" class="text-center">No document found.</td>
                </tr>
            }
        </tbody>
    </table>

</body>
</html>

<script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>

*@


@model IEnumerable<CompanyDocumentsViewModel>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Company Documents Management</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="max-w-4xl mx-auto mt-10 bg-white p-6 rounded-lg shadow-md">
        <h2 class="text-xl font-semibold text-gray-800 mb-6 text-center">Company Documents Management</h2>

        <form method="post" action="@Url.Action("Index")" class="space-y-4">
            <div>
                <label for="employeeCode" class="block text-sm font-medium text-gray-700">Employee</label>
                <select id="employeeCode" name="Emp_Code" class="w-full mt-1 p-2 border rounded-md">
                    <option value="">Select Employee</option>
                    @if (ViewBag.Employees != null)
                    {
                        foreach (var employee in ViewBag.Employees)
                        {
                            <option value="@employee.Emp_Code">@employee.Emp_Code</option>
                        }
                    }
                </select>
            </div>

            <div>
                <label for="documentType" class="block text-sm font-medium text-gray-700">Document Type</label>
                <select id="documentType" name="DocumentType" required class="w-full mt-1 p-2 border rounded-md">
                    <option value="">-- Select Document Type --</option>
                    <option value="Company UAN">Company UAN</option>
                    <option value="GST/MOI/COI/PAN/TAN/ICC">GST/MOI/COI/PAN/TAN/ICC</option>
                    <option value="Rental Agreements">Rental Agreements</option>
                    <option value="Broadband services">Broadband services</option>
                    <option value="Coffee/Tea vendors">Coffee/Tea vendors</option>
                    <option value="House keeping">House keeping</option>
                    <option value="Stationary">Stationary</option>
                    <option value="Welcome kit">Welcome kit</option>
                    <option value="Laptop">Laptop</option>
                    <option value="Electricity">Electricity</option>
                    <option value="Water Bill">Water Bill</option>
                    <option value="AC Bill">AC Bill</option>
                    <option value="Rental Bills">Rental Bills</option>
                    <option value="Others">Others</option>
                </select>
            </div>

            <div class="text-center">
                <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-md shadow hover:bg-blue-700">
                    <i class="fas fa-filter"></i> Apply Filters
                </button>
            </div>
        </form>
    </div>

    <div class="max-w-5xl mx-auto mt-6">
        <div class="overflow-x-auto bg-white shadow-md rounded-lg">
            <table class="w-full table-auto border-collapse">
                <thead class="bg-gray-200">
                    <tr>
                        <th class="px-4 py-2">Id</th>
                        <th class="px-4 py-2">Employee Code</th>
                        <th class="px-4 py-2">Document Type</th>
                        <th class="px-4 py-2">File Type</th>
                        <th class="px-4 py-2">File Name</th>
                        <th class="px-4 py-2">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var task in Model)
                        {
                            <tr class="border-b">
                                <td class="px-4 py-2">@task.Id</td>
                                <td class="px-4 py-2">@task.Emp_Code</td>
                                <td class="px-4 py-2">@task.DocumentType</td>
                                <td class="px-4 py-2">@task.FileType</td>
                                <td class="px-4 py-2">@task.FileName</td>
                                <td class="px-4 py-2 flex space-x-2">
                                    <form method="post" asp-action="ViewDocument">
                                        <input type="hidden" name="id" value="@task.Id" />
                                        <button type="submit" class="bg-gray-600 text-white px-3 py-1 rounded-md hover:bg-gray-700">
                                            <i class="bi bi-eye"></i> View
                                        </button>
                                    </form>
                                    <form method="post" asp-action="DownloadDocument">
                                        <input type="hidden" name="id" value="@task.Id" />
                                        <button type="submit" class="bg-indigo-600 text-white px-3 py-1 rounded-md hover:bg-indigo-700">
                                            <i class="bi bi-file-earmark-arrow-down"></i> Download
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="text-center py-4">No document found.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
