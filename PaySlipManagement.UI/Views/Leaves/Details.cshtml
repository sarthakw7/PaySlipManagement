@model LeavesViewModel

<h1>Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Id</dt>
        <dd class="col-sm-10">@Model.Id</dd>

        <dt class="col-sm-2">Employee Code</dt>
        <dd class="col-sm-10">@Model.Emp_Code</dd>

        <dt class="col-sm-2">Type Id</dt>
        <dd class="col-sm-10">@Model.TypeId</dd>

        <dt class="col-sm-2">Total Leaves</dt>
        <dd class="col-sm-10">@Model.TotalLeaves</dd>

        <dt class="col-sm-2">Leaves Available</dt>
        <dd class="col-sm-10">@Model.LeavesAvailable</dd>

        <dt class="col-sm-2">Leaves Used</dt>
        <dd class="col-sm-10">@Model.LeavesUsed</dd>
    </dl>
</div>
<a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edit</a> |
<a asp-action="Index" class="btn btn-secondary">Back to List</a>
